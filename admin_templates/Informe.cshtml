
@using Newtonsoft.Json;
@{
    

bool consulta = Convert.ToBoolean(Request.QueryString["consulta"]);
bool reporte = Convert.ToBoolean(Request.QueryString["reporte"]);

/*if(consulta==true){

    Write("Consultando");

}else if(reporte==true){

   Write("Reportando");
}*/


}




@if(consulta==true){

    <text>
        <strong>Write</strong>
    </text>

}else if(reporte==true){

     <text> 
        Writemmmm
    </text>
}

@{
    //<text>@Request.QueryString["fecha"]</text>
   // <text>@Request.QueryString["fecha"].AsDateTime()</text>
   var inputs = Request.QueryString["inputs"];
   var js = new JsonTextReader(new StringReader(inputs));
   var son = @JsonConvert.DeserializeObject(inputs);
   //var b = son.b;
   string[] input = new string[6]; 
   int i; 
   i=0;
   //input[i]="";
   /*while(js.Read()){
        input[i]="lll"; 
        //var t = js.Value;
         i++; 
         <text>@js.Value</text>
        }
    <text>@js</text>*/
    <text>@inputs</text>
}

